import React from 'react';

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-surface">
      <div className="text-center p-6">
        <h1 className="text-3xl font-bold mb-4">Creative Professional Portfolio</h1>
        <p className="text-text-secondary mb-2">Welcome — this is a minimal React + Vite setup.</p>
        <p className="text-sm text-text-muted">Your existing Tailwind CSS is imported from `css/main.css`.</p>
      </div>
    </div>
  );
}
